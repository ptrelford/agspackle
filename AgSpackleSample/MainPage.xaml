<UserControl x:Class="AgSpackleSample.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">

    <Grid x:Name="LayoutRoot" Background="Green">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Button Height="64" Margin="8" Click="NewTransparentWindowButton_Click">
                <TextBlock TextWrapping="Wrap">Launch New Transparent Window</TextBlock>
            </Button>
            <Button Height="64" Margin="8" Click="NewModalDialogWindowButton_Click">
                <TextBlock TextWrapping="Wrap">Show Modal Window</TextBlock>
            </Button>
            <TextBlock x:Name="textBlock1"></TextBlock>
        </StackPanel>
        <ListBox Name="Events" Margin="8" Grid.Column="2">
        </ListBox>
    </Grid>
</UserControl>
